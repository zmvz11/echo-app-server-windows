# Contributing

Use feature branches and pull requests.

Required before merge:

```bash
npm run lint
npm run typecheck
npm run build
```

Naming must stay within the Echo App Center / Echo App Server project model.

Accounts must remain username-only.
